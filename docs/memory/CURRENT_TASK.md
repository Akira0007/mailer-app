# CURRENT_TASK

## 当前任务

Bootstrap 完成。

## 已完成

- 重构到三层目录结构
- 代码已提交到 GitHub
- README 已替换为项目说明
- `docs/memory/` 已建立
- `app.ping` 的 typed IPC 已接通

## 下一步

先做联系人导入的最小数据结构和 `shared` 类型。

## 注意

每次只做一个小任务，不要同时扩多个模块。
