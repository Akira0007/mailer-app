# HANDOFF

## 给下一个执行者

当前项目已经可以正常启动。
当前项目已经可以正常启动，并且 `app.ping` 的 typed IPC 已打通。

## 建议接手顺序

1. 再补联系人导入基础能力
2. 再补 SMTP 账号和测试发送
3. 再补发送队列

## 交接规则

- 一次只做一个小闭环
- 先改 shared 类型，再改 main，再改 preload，最后改 renderer
- 改完立刻跑验证命令
