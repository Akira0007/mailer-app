# macOS 图形化邮件群发客户端团队技术规划

> 目标读者：产品负责人、开发新手、AI 协作团队、后续负责上架和商业化的成员。  
> 当前建议技术栈：Electron + TypeScript + React + SQLite + SMTP + StoreKit/macOS App Store。  
> 文档日期：2026-05-20。

## 1. 产品定位

你要做的不是一个普通邮箱客户端，而是一个面向销售、外贸、招聘、私域运营、小团队 CRM 场景的“合规邮件触达工具”。

核心能力：

1. 从 Excel 导入邮箱联系人。
2. 添加多个 SMTP 发送邮箱。
3. 按设定频率逐个发送邮件。
4. 多个发送邮箱轮流发送，避免单个邮箱压力过大。
5. 支持免费版限制和付费版解锁。
6. 后续加入邮件模板、签名、HTML 导入、AI 生成内容。
7. 后续与“AI 获客 + CRM”系列软件共享数据。

专业建议：不要把产品描述成“群发垃圾邮件工具”。从第一天开始就把产品设计为“合规邮件触达、客户关系维护、销售线索跟进工具”。这会影响 App Store 审核、用户信任、邮件送达率和法律风险。

## 2. 合规与风险提醒

邮件群发产品最容易踩的坑不是代码，而是滥发、退订、隐私和账号风控。

最低限度应该内置这些功能：

1. 每个联系人要有来源字段，例如“Excel 导入、手动添加、CRM 同步、网页表单”等。
2. 每封营销邮件应支持退订链接或退订邮箱。
3. 系统要有 suppression list，即退订/拉黑名单，发送前必须自动过滤。
4. 记录每次发送日志，包括发送账号、收件人、模板、时间、状态、错误信息。
5. 不要伪造发件人、不要误导主题、不要隐藏身份。
6. 用户导入联系人时，要提示其确认拥有合法发送依据。

如果主要面向美国市场，需要关注 CAN-SPAM。FTC 官方说明强调商业邮件需要遵守商业邮件规则、给收件人停止接收的权利，并对违规有处罚。参考：[FTC CAN-SPAM 合规指南](https://www.ftc.gov/business-guidance/resources/can-spam-act-compliance-guide-business?_rsc=i4914)。

如果面向欧盟，需要另外考虑 GDPR 和 ePrivacy。中国市场也要注意个人信息保护法、广告法和反垃圾信息要求。建议在产品中加入“合规发送模式”，而不是把这些留到后面补。

## 3. 推荐总体架构

建议采用三层架构，并且从第一版就模块化：

```text
UI 层
  Electron Renderer / React / 页面组件 / 表单 / 图表 / 设置页

应用逻辑层
  联系人导入服务
  发送任务服务
  SMTP 账号服务
  权限与套餐服务
  模板服务
  统计服务
  AI 服务适配器

数据层
  SQLite 本地数据库
  文件存储
  加密凭证存储
  导入导出协议
```

Electron 内部再分成：

```text
Electron Main Process
  负责系统能力、文件选择、数据库、SMTP、队列、加密、StoreKit 桥接

Electron Renderer Process
  负责界面展示和用户交互

Preload Script
  只暴露安全 IPC API，禁止 renderer 直接访问 Node.js 敏感能力
```

## 4. 为什么适合用 Electron

优点：

1. 对开发新手友好，前端技术栈资料多。
2. macOS、Windows 后期都能复用大量代码。
3. UI 开发效率高，适合复杂表格、编辑器、模板预览。
4. 可以用 Node.js 生态处理 Excel、SMTP、SQLite、HTML 模板。

缺点：

1. 体积比原生 Swift App 大。
2. 上架 Mac App Store 需要处理 sandbox、签名、entitlements、StoreKit 集成。
3. 如果安全边界设计不好，容易有本地数据泄露风险。

结论：MVP 用 Electron 是合理的。后期如果极度追求原生体验，再考虑 SwiftUI 重写部分能力，不建议一开始就用 SwiftUI 承担全部业务复杂度。

## 5. MVP 第一版功能范围

第一版不要做太大。建议只做这些：

1. 联系人 Excel 导入。
2. 联系人列表查看、搜索、去重、删除。
3. 添加一个或多个 SMTP 账号。
4. SMTP 连接测试。
5. 简单邮件编辑：主题、纯文本正文、简单 HTML 正文。
6. 创建发送任务。
7. 设置发送间隔，例如每 30 秒发送 1 封。
8. 多 SMTP 账号轮流发送。
9. 发送日志和失败重试。
10. 免费版限制框架：每天 500 封、1 个 SMTP、500 个联系人。

暂时不要在 MVP 做：

1. 复杂拖拽模板编辑器。
2. 完整 CRM。
3. 复杂 AI 代理。
4. 高级自动化工作流。
5. 多端云同步。

这些都要预留接口，但不应该塞进第一版。

## 6. 功能模块拆分

### 6.1 联系人模块

职责：

1. Excel/CSV 导入。
2. 字段映射，例如 name、email、company、phone、tags。
3. 邮箱格式校验。
4. 重复联系人识别。
5. 标签管理。
6. 退订状态管理。
7. 导出联系人给 CRM 或其他系列软件。

推荐数据字段：

```text
contacts
  id
  email
  name
  company
  title
  phone
  source
  tags
  status: active | unsubscribed | bounced | blocked
  created_at
  updated_at
```

### 6.2 SMTP 账号模块

职责：

1. 添加 SMTP 服务器。
2. 测试连接。
3. 保存发件人名称、邮箱、host、port、secure、auth。
4. 凭证加密保存。
5. 每个 SMTP 账号设置每日上限、每小时上限、冷却时间。
6. 发送失败后自动暂停账号。

密码不要明文存在 SQLite。建议使用 macOS Keychain，或者至少使用 Electron safeStorage 加密。更好的做法是建立 `CredentialStore` 接口，当前实现用本机安全存储，后面可以替换。

### 6.3 邮件发送模块

职责：

1. 创建发送任务。
2. 根据联系人列表生成收件队列。
3. 根据频率限制逐个发送。
4. 多 SMTP 账号轮询发送。
5. 失败重试。
6. 暂停、继续、取消。
7. 记录发送日志。

核心设计：发送不是点击按钮后直接循环发完，而是进入队列。

```text
campaign -> send_jobs -> send_attempts
```

这样后面才能支持暂停、重试、统计、断点续发。

### 6.4 模板模块

第一版只做轻量：

1. 邮件主题。
2. 正文 HTML。
3. 变量占位符，例如 `{{name}}`、`{{company}}`。
4. 发送前预览。

后续扩展：

1. HTML 导入。
2. 可视化模板编辑器。
3. 签名编辑器。
4. AI 生成模板。
5. A/B 测试。

### 6.5 统计模块

第一版统计：

1. 已发送数量。
2. 成功数量。
3. 失败数量。
4. 每个 SMTP 账号发送数量。
5. 每个任务进度。

后续统计：

1. 打开率。
2. 点击率。
3. 回复率。
4. 退订率。
5. Bounce 退信率。

注意：打开率需要插入跟踪像素，点击率需要链接重定向，这会引入服务器端服务和隐私合规问题。MVP 不建议做。

## 7. 权限体系如何从现在预留

你后面要做免费版和付费版，所以第一天就不要在代码里到处写死：

```ts
if (isPaid) {
  // unlimited
}
```

应该设计一个统一的权限服务：

```ts
type FeatureKey =
  | "contacts.max_count"
  | "smtp_accounts.max_count"
  | "emails.daily_send_limit"
  | "templates.html_import"
  | "templates.ai_generate"
  | "signatures.max_count"
  | "crm_export"
  | "ai_provider.custom_key";
```

套餐配置：

```ts
const PLANS = {
  free: {
    "contacts.max_count": 500,
    "smtp_accounts.max_count": 1,
    "emails.daily_send_limit": 500,
    "templates.html_import": false,
    "templates.ai_generate": false,
    "signatures.max_count": 1,
    "crm_export": false,
    "ai_provider.custom_key": false
  },
  pro: {
    "contacts.max_count": Infinity,
    "smtp_accounts.max_count": Infinity,
    "emails.daily_send_limit": Infinity,
    "templates.html_import": true,
    "templates.ai_generate": true,
    "signatures.max_count": Infinity,
    "crm_export": true,
    "ai_provider.custom_key": true
  }
};
```

所有限制都通过 `EntitlementService` 判断：

```ts
canAddContact()
canAddSmtpAccount()
canSendToday(count)
canUseHtmlImport()
canUseAiGenerate()
```

这样以后你改套餐、加企业版、加买断版，不需要到处重写业务代码。

## 8. App Store 和内购设计

如果你要上架 Mac App Store，并在 App 内卖数字功能解锁，一般应使用 Apple In-App Purchase / StoreKit。苹果官方 StoreKit 文档说明可以在 Apple 平台提供 App 内购买；macOS App Store 分发还需要启用 App Sandbox。参考：

1. [Apple In-App Purchase 文档](https://developer.apple.com/documentation/storekit/in-app-purchase?language=objc)
2. [Offering, completing, and restoring in-app purchases](https://developer.apple.com/documentation/storekit/offering-completing-and-restoring-in-app-purchases)
3. [Apple App Sandbox 文档](https://developer.apple.com/documentation/Security/app-sandbox)
4. [Configuring the macOS App Sandbox](https://developer.apple.com/documentation/xcode/configuring-the-macos-app-sandbox?changes=latest_be_1)
5. [App Store Review Guidelines](https://developer.apple.com/app-store/review/guidelines/)

建议内购方案：

```text
免费版
  每天最多发送 500 封
  最多 1 个 SMTP 服务器
  最多 500 个联系人
  基础模板

Pro 月订阅
  无限联系人
  无限 SMTP 账号
  无限发送额度，仍受用户 SMTP 服务商限制
  HTML 导入
  AI 模板生成
  多签名
  CRM 导入导出

Pro 年订阅
  同 Pro 月订阅，价格更优惠
```

实现建议：

1. Electron 负责 UI 和业务。
2. StoreKit 通过 macOS 原生 Swift helper 或 Electron 原生桥接实现。
3. 主进程通过 IPC 暴露 `purchasePro()`、`restorePurchases()`、`getEntitlements()`。
4. 本地保存订阅状态缓存，但关键状态要能恢复购买。
5. App 启动时校验本地收据或 StoreKit Transaction 状态。

不要把“无限发送”理解成真的可以无限制轰炸。UI 上应该写清楚：付费版解除软件内限制，但实际发送能力仍受 SMTP 服务商、邮箱提供商和合规规则限制。

## 9. macOS Sandbox 和权限注意事项

Mac App Store 要求 App Sandbox。邮件客户端需要至少考虑：

1. 网络客户端权限：用于连接 SMTP 服务器。
2. 文件访问：用户选择 Excel 文件时，通过系统文件选择器获取授权。
3. 本地数据库：放在应用容器目录。
4. 凭证：优先使用 Keychain。

不要默认扫描用户整个硬盘。导入 Excel 必须让用户主动选择文件。这样更符合 macOS 安全模型，也更容易通过审核。

## 10. Electron 项目结构建议

推荐结构：

```text
mail-client/
  package.json
  electron.vite.config.ts
  tsconfig.json

  src/
    main/
      index.ts
      ipc/
        contacts.ipc.ts
        smtp.ipc.ts
        campaigns.ipc.ts
        entitlements.ipc.ts
      services/
        contacts/
          ContactImportService.ts
          ContactRepository.ts
        smtp/
          SmtpAccountService.ts
          SmtpSender.ts
          SmtpCredentialStore.ts
        campaigns/
          CampaignService.ts
          SendQueueService.ts
          RateLimiter.ts
          SenderRotationPolicy.ts
        templates/
          TemplateService.ts
          VariableRenderer.ts
        entitlements/
          EntitlementService.ts
          PlanConfig.ts
        ai/
          AiProvider.ts
          OpenAIProvider.ts
          GeminiProvider.ts
          AnthropicProvider.ts
        storekit/
          StoreKitBridge.ts
      db/
        schema.sql
        migrations/
        sqlite.ts

    preload/
      index.ts

    renderer/
      app/
        App.tsx
        routes.tsx
      pages/
        ContactsPage.tsx
        SmtpAccountsPage.tsx
        CampaignsPage.tsx
        TemplatesPage.tsx
        SettingsPage.tsx
        BillingPage.tsx
      components/
      styles/

  tests/
    unit/
    integration/
    e2e/

  docs/
    CONTEXT.md
    ADR/
    product/
    ai-memory/
```

## 11. 数据库表设计草案

```sql
contacts(
  id TEXT PRIMARY KEY,
  email TEXT NOT NULL,
  name TEXT,
  company TEXT,
  title TEXT,
  phone TEXT,
  source TEXT,
  status TEXT NOT NULL,
  created_at TEXT NOT NULL,
  updated_at TEXT NOT NULL
);

smtp_accounts(
  id TEXT PRIMARY KEY,
  display_name TEXT NOT NULL,
  email TEXT NOT NULL,
  host TEXT NOT NULL,
  port INTEGER NOT NULL,
  secure INTEGER NOT NULL,
  username TEXT NOT NULL,
  credential_ref TEXT NOT NULL,
  daily_limit INTEGER,
  hourly_limit INTEGER,
  cooldown_seconds INTEGER,
  status TEXT NOT NULL,
  created_at TEXT NOT NULL,
  updated_at TEXT NOT NULL
);

templates(
  id TEXT PRIMARY KEY,
  name TEXT NOT NULL,
  subject TEXT NOT NULL,
  html_body TEXT,
  text_body TEXT,
  created_at TEXT NOT NULL,
  updated_at TEXT NOT NULL
);

campaigns(
  id TEXT PRIMARY KEY,
  name TEXT NOT NULL,
  template_id TEXT NOT NULL,
  status TEXT NOT NULL,
  send_interval_seconds INTEGER NOT NULL,
  created_at TEXT NOT NULL,
  updated_at TEXT NOT NULL
);

send_jobs(
  id TEXT PRIMARY KEY,
  campaign_id TEXT NOT NULL,
  contact_id TEXT NOT NULL,
  status TEXT NOT NULL,
  scheduled_at TEXT,
  sent_at TEXT,
  last_error TEXT
);

send_attempts(
  id TEXT PRIMARY KEY,
  send_job_id TEXT NOT NULL,
  smtp_account_id TEXT NOT NULL,
  status TEXT NOT NULL,
  error_message TEXT,
  created_at TEXT NOT NULL
);

usage_counters(
  id TEXT PRIMARY KEY,
  date TEXT NOT NULL,
  metric TEXT NOT NULL,
  value INTEGER NOT NULL
);

entitlements(
  key TEXT PRIMARY KEY,
  value TEXT NOT NULL,
  source TEXT NOT NULL,
  updated_at TEXT NOT NULL
);
```

## 12. 发送队列和轮流发送逻辑

发送逻辑建议这样走：

```text
用户创建 campaign
  -> 系统读取目标联系人
  -> 过滤无效、重复、退订、拉黑联系人
  -> 生成 send_jobs
  -> SendQueueService 按间隔取下一条 job
  -> EntitlementService 检查今日额度
  -> SenderRotationPolicy 选择 SMTP 账号
  -> SmtpSender 发送
  -> 写入 send_attempts 和 send_jobs 状态
  -> 更新 usage_counters
```

轮流发送策略：

1. 简单轮询：A、B、C、A、B、C。
2. 加权轮询：高额度账号发更多。
3. 健康状态过滤：失败率高的账号暂时跳过。
4. 限速策略：每个账号独立计算冷却时间。

MVP 先做简单轮询 + 每账号冷却时间。

## 13. Electron 如何测试邮件发送

不要一开始就用真实邮箱测试群发。测试要分四层：

### 13.1 单元测试

测试不连网络的逻辑：

1. Excel 字段解析。
2. 邮箱格式校验。
3. 权限限制。
4. 轮询算法。
5. 限速算法。
6. 模板变量替换。

工具：Vitest。

### 13.2 SMTP 假服务器测试

用本地 SMTP 测试服务器接收邮件，不真正发出去：

1. Mailpit
2. MailHog
3. smtp4dev

推荐 Mailpit。启动后 Electron 连接本地 SMTP，例如：

```text
host: 127.0.0.1
port: 1025
secure: false
```

然后在 Mailpit Web UI 查看收到的邮件。

测试点：

1. 是否真的发出一封邮件。
2. 主题是否正确。
3. HTML 是否正确。
4. 收件人是否正确。
5. 多 SMTP 轮询是否符合预期。
6. 频率限制是否生效。

### 13.3 集成测试

用测试数据库 + 假 SMTP：

1. 导入一个 Excel。
2. 创建一个 campaign。
3. 设置 2 个 SMTP。
4. 设置每 1 秒发送 1 封。
5. 等待队列完成。
6. 检查 Mailpit 收到的数量。
7. 检查数据库日志状态。

### 13.4 E2E 测试

用 Playwright 测 Electron 界面：

1. 打开 App。
2. 导入测试 Excel。
3. 添加 SMTP。
4. 创建邮件任务。
5. 点击开始发送。
6. 检查 UI 进度和日志。

## 14. 开发小白用 Codex 的步骤

推荐你按阶段推进，不要一次让 AI 写完整大系统。

### 阶段 0：建立项目知识库

先创建：

```text
docs/
  CONTEXT.md
  product/
    PRD.md
    roadmap.md
  architecture/
    overview.md
    database.md
    permissions.md
  ADR/
    0001-use-electron.md
    0002-use-sqlite.md
    0003-use-local-smtp-queue.md
  ai-memory/
    decisions.md
    glossary.md
    open-questions.md
    task-log.md
```

`CONTEXT.md` 是 AI 共享记忆入口，要写清楚：

1. 这个产品是什么。
2. 不是什么。
3. 技术栈。
4. 模块边界。
5. 当前决策。
6. 不允许随意改的规则。

### 阶段 1：搭 Electron 空项目

让 Codex 做：

1. 创建 Electron + React + TypeScript 项目。
2. 配置 ESLint、Prettier、Vitest。
3. 跑通开发服务器。
4. 做一个基本导航。

示例提示词：

```text
请创建一个 Electron + React + TypeScript 项目，使用 electron-vite。
先只做基本窗口、左侧导航和 5 个页面占位：
联系人、SMTP账号、发送任务、模板、设置。
不要实现业务逻辑。完成后运行测试和开发服务器。
```

### 阶段 2：先做数据库和联系人导入

让 Codex 做：

```text
请实现 SQLite 数据库层和联系人模块。
支持从 xlsx 导入联系人，字段包括 email/name/company/tags。
导入时校验 email、去重，并写入 contacts 表。
请补充单元测试。
```

### 阶段 3：SMTP 账号和测试连接

让 Codex 做：

```text
请实现 SMTP 账号模块。
支持添加 SMTP host/port/secure/username/password/fromName/fromEmail。
密码不要明文入库，请抽象 CredentialStore。
实现测试连接功能，并用 Mailpit 写集成测试。
```

### 阶段 4：发送队列

让 Codex 做：

```text
请实现 CampaignService、SendQueueService、RateLimiter、SenderRotationPolicy。
要求根据联系人生成 send_jobs，以固定间隔逐个发送。
多个 SMTP 账号按轮询策略发送。
失败要记录 send_attempts，任务可暂停/继续/取消。
```

### 阶段 5：权限体系

让 Codex 做：

```text
请实现 EntitlementService。
免费版限制：每天 500 封、最多 1 个 SMTP、最多 500 个联系人。
所有业务模块调用 EntitlementService，不要在 UI 里写死限制。
付费版暂时用本地 mock entitlement，后续接 StoreKit。
```

### 阶段 6：App Store 和 StoreKit

最后再做：

1. Apple Developer 账号。
2. App ID。
3. Bundle ID。
4. Mac App Sandbox。
5. StoreKit 商品。
6. 购买、恢复购买、订阅状态校验。
7. 打包、签名、公证或 MAS 构建。

## 15. Google AI Studio 如何加入 UI 工作流

Google AI Studio 不建议直接作为代码主力，而是负责视觉探索和界面方案。

推荐分工：

1. 用 Google AI Studio 生成 UI 风格参考。
2. 让它输出页面结构、组件清单、颜色、间距、交互说明。
3. 把结果保存到 `docs/product/ui-spec.md`。
4. 再让 Codex 根据 `ui-spec.md` 实现 React 页面。
5. 用截图或浏览器检查 UI，发现问题再回到 Codex 修改。

UI 页面优先级：

1. 联系人导入页。
2. SMTP 账号设置页。
3. 创建发送任务页。
4. 发送进度页。
5. 设置与订阅页。
6. 模板编辑页。

给 Google AI Studio 的提示词示例：

```text
请为一款 macOS 上的 B2B 邮件触达客户端设计 UI。
风格要求：专业、克制、接近 macOS 桌面软件，不要营销落地页风格。
核心页面包括：联系人导入、SMTP 账号、发送任务、模板编辑、统计、订阅。
请输出每个页面的布局、组件、状态、颜色建议和交互细节。
```

## 16. 图片里的“共享记忆”方法如何落地

图片里的核心思想是：不要让每个 AI 只依赖自己的上下文窗口，而是把长期记忆放到一个共同可读写的地方。

推荐落地方式：

```text
Obsidian Vault / 项目 docs 目录
  作为共享记忆仓库

Claude
  负责需求梳理、方案审查、文档整理

Codex
  负责代码实现、测试、修 bug

Google AI Studio
  负责 UI 视觉探索

MCP / 文件系统
  让 AI 读写同一套 Markdown 文档
```

### 16.1 是否需要项目知识库

需要。尤其你这个项目后面会有：

1. 邮件客户端。
2. AI 获客 + CRM。
3. 模板编辑器。
4. AI 生成内容。
5. 多套餐权限。
6. 数据互通。

如果没有知识库，不同 AI 会反复忘记决策，代码会越来越乱。

### 16.2 推荐知识库结构

```text
project-memory/
  00-start-here.md
  01-product-vision.md
  02-roadmap.md
  03-domain-glossary.md
  04-current-decisions.md
  05-open-questions.md
  06-ai-collaboration-rules.md

  product/
    prd-mail-client.md
    prd-ai-leads-crm.md
    pricing-and-plans.md

  architecture/
    system-overview.md
    module-boundaries.md
    database-schema.md
    entitlement-system.md
    import-export-protocol.md

  adr/
    0001-electron.md
    0002-sqlite.md
    0003-local-first-data.md
    0004-storekit-for-mas.md

  meetings/
    yyyy-mm-dd-notes.md

  tasks/
    backlog.md
    current-sprint.md
```

### 16.3 AI 共享记忆规则

建议写进 `06-ai-collaboration-rules.md`：

```text
1. 所有 AI 开始工作前先读 00-start-here.md、04-current-decisions.md、05-open-questions.md。
2. 做出架构决策后，必须写入 adr/。
3. 修改模块边界后，必须更新 architecture/module-boundaries.md。
4. 完成功能后，必须更新 tasks/current-sprint.md。
5. 不确定的问题写入 open-questions，不要假装已经决定。
6. Codex 负责实现，Claude 负责审查和需求澄清。
7. UI 视觉方案来自 Google AI Studio，但最终以代码实现和可用性测试为准。
```

### 16.4 MCP / 文件系统怎么做

最简单版本：

1. 把 Obsidian Vault 放在项目目录旁边，或直接用项目 `docs/`。
2. Claude、Codex 都能访问同一个文件夹。
3. 每次工作前让 AI 读取关键 Markdown。
4. 每次工作后让 AI 更新决策和任务日志。

进阶版本：

1. 使用 filesystem MCP，让 Claude/Codex 能读写同一目录。
2. 使用 Git 管理知识库。
3. 重要决策用 Pull Request 或 commit 记录。
4. 每周让 AI 生成一次 `weekly-summary.md`。

## 17. 多 AI 协作分工

推荐顺序：

```text
1. Claude Code：先审需求和架构
2. Codex：实现代码和测试
3. Claude Code：Review 代码和设计一致性
4. Obsidian：记录决策、问题、任务状态
5. MCP：连接 Claude、Codex、Obsidian
```

具体分工：

```text
Claude
  写 PRD
  评审架构
  拆任务
  检查逻辑漏洞
  帮你把小白语言翻译成工程需求

Codex
  搭项目
  写代码
  改 bug
  跑测试
  做 Electron、SQLite、SMTP、StoreKit 集成

Google AI Studio
  生成 UI 风格方向
  输出视觉规范
  生成页面草图说明

Obsidian
  存知识库
  存会议纪要
  存 ADR
  存模块边界

MCP / 文件系统
  让 AI 读写共享记忆
```

## 18. 系列软件之间的数据互通

你后面还有一个“AI 获客 + CRM”，所以现在要提前定义共同数据协议。

建议所有软件共用这些核心概念：

```text
Contact
  联系人

Company
  公司

Lead
  线索

Campaign
  触达活动

Message
  邮件或其他消息

Interaction
  互动记录

Tag
  标签

Suppression
  退订/黑名单
```

导入导出格式：

1. CSV：给普通用户。
2. XLSX：给 Excel 用户。
3. JSON：给系列软件内部互通。
4. SQLite export：高级备份。

推荐定义一个内部 JSON 协议：

```json
{
  "schemaVersion": "1.0.0",
  "app": "mail-client",
  "exportedAt": "2026-05-20T00:00:00Z",
  "contacts": [],
  "companies": [],
  "campaigns": [],
  "tags": [],
  "suppressions": []
}
```

第一版就要加 `schemaVersion`，以后才能平滑升级。

## 19. AI API 供应商可更换设计

不要把 OpenAI、Gemini、Claude 的调用写死在模板模块里。要做 Provider 接口：

```ts
interface AiProvider {
  id: string;
  name: string;
  generateEmailTemplate(input: GenerateTemplateInput): Promise<GenerateTemplateResult>;
  generateSignature(input: GenerateSignatureInput): Promise<GenerateSignatureResult>;
}
```

实现：

```text
OpenAIProvider
GeminiProvider
AnthropicProvider
LocalProvider
CustomOpenAICompatibleProvider
```

用户设置：

1. 选择供应商。
2. 填 API Key。
3. 填 Base URL，可选。
4. 选择模型。
5. 测试连接。

免费版是否允许自带 API Key，需要商业上决定。建议：

1. 免费版允许基础模板手写。
2. Pro 版开放 AI 生成。
3. 企业版开放自定义 Provider 和团队共享 Key。

## 20. 开发里程碑

### Milestone 1：项目骨架

交付：

1. Electron + React + TypeScript。
2. 基本导航。
3. SQLite 初始化。
4. 测试框架。

### Milestone 2：联系人导入

交付：

1. Excel 导入。
2. 联系人表格。
3. 去重和校验。
4. 免费版 500 联系人限制。

### Milestone 3：SMTP 账号

交付：

1. 添加 SMTP。
2. 测试连接。
3. 凭证加密。
4. 免费版 1 个 SMTP 限制。

### Milestone 4：发送任务

交付：

1. 创建 campaign。
2. 简单邮件编辑。
3. 队列发送。
4. 多 SMTP 轮询。
5. 发送日志。

### Milestone 5：权限和订阅准备

交付：

1. EntitlementService。
2. 本地 mock Pro。
3. Billing 页面。
4. StoreKit 接入设计。

### Milestone 6：模板和 AI

交付：

1. 模板管理。
2. HTML 导入。
3. 签名。
4. AI Provider 接口。
5. Gemini/OpenAI 等实现。

### Milestone 7：App Store 上架

交付：

1. App 图标。
2. Sandbox 配置。
3. StoreKit 商品。
4. 签名打包。
5. 审核材料。
6. 隐私政策。
7. 用户协议。

## 21. 给团队的执行建议

第一周目标：

1. 建好项目知识库。
2. 明确 MVP 范围。
3. 搭 Electron 项目。
4. 跑通 SQLite。
5. 做联系人导入原型。

第二周目标：

1. SMTP 账号管理。
2. Mailpit 测试邮件发送。
3. 发送队列原型。
4. 权限服务初版。

第三周目标：

1. 完整发送流程。
2. UI 打磨。
3. 发送日志。
4. 失败重试。
5. 免费版限制。

第四周目标：

1. 内测版本。
2. 打包 macOS App。
3. 安全和合规检查。
4. 准备 StoreKit 和 App Store 材料。

## 22. 最重要的专业建议

1. 先做“可靠发送”，不要先做“漂亮模板”。
2. 从第一天建立权限服务，不然后面补内购会很痛苦。
3. 从第一天记录发送日志，不然后面无法排查问题。
4. 从第一天处理退订和黑名单，不然产品风险很高。
5. 从第一天定义导入导出协议，不然后面和 CRM 打通会返工。
6. 不要让 AI 直接在没有项目记忆的情况下乱写代码。
7. 每个模块都要有边界：联系人、SMTP、发送、模板、统计、权限、AI。
8. MVP 要小，架构要可扩展。

## 23. 下一步可以直接让 Codex 做什么

你可以把下面这段作为下一次给 Codex 的任务：

```text
请基于这份规划文档，创建一个 Electron + React + TypeScript + SQLite 的 macOS 邮件触达客户端项目骨架。
要求：
1. 使用模块化目录结构。
2. 创建联系人、SMTP账号、发送任务、模板、设置、订阅页面占位。
3. 创建数据库 schema 和 migration 机制。
4. 创建 EntitlementService，先实现免费版限制配置。
5. 创建 docs/CONTEXT.md 和 ADR 目录。
6. 不要实现真实邮件发送，先保证项目能运行、测试能通过。
```

